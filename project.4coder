version(1);

project_name = "Handmade Game";

patterns = {
"*.cpp",
"*.m",
"*.mm",
"*.h",
"*.bat",
"*.sh",
"*.4coder",
};

blacklist_patterns = {
".*",
};

load_paths_win = { 
 { ".", .relative = true, .recursive = true, },
};

load_paths_unix = {
  { ".", .relative = true, .recursive = true, },
};

load_paths = {
 { load_paths_win, .os = "win" },
 { load_paths_unix, .os = "mac" },
};
